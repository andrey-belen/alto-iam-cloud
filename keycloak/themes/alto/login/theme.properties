# Alto Login Theme - Professional Custom Design
parent=keycloak
import=common/keycloak

# Custom styles
styles=css/alto.css

# Custom properties
meta=viewport==width=device-width,initial-scale=1

# Request access URL (for the "Need access?" link)
requestAccessUrl=http://localhost:3000/request-access
